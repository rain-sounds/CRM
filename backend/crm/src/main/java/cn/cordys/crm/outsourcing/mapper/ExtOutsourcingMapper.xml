<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.cordys.crm.outsourcing.mapper.ExtOutsourcingMapper">
    <select id="list" resultType="cn.cordys.crm.outsourcing.dto.response.OutsourcingListResponse">
        select o.id, o.internal_project_no, o.project_source, o.experiment_content,
        o.experiment_type, o.outsourcing_vendor, o.deal_price, o.outsourcing_amount,
        o.outsourcing_time, o.result_return_time, o.settlement_time, o.settlement_amount,
        o.follow_up_process, o.owner, o.create_time, o.update_time, o.create_user, o.update_user
        from outsourcing o

        <include refid="fieldConditionJoin">
            <property name="conditions" value="request.filters"/>
            <property name="tablePrefix" value="filter"/>
        </include>
        <include refid="fieldConditionJoin">
            <property name="conditions" value="request.combineSearch.conditions"/>
            <property name="tablePrefix" value="combine"/>
        </include>
        <include refid="fieldConditionJoin">
            <property name="conditions" value="request.viewCombineSearch.conditions"/>
            <property name="tablePrefix" value="viewCombine"/>
        </include>
        <include refid="fieldSortJoin">
            <property name="sort" value="request.sort"/>
        </include>

        <if test="dataPermission != null and dataPermission.deptIds.size() > 0">
            join sys_organization_user sou on o.owner = sou.user_id
            and (
            sou.department_id in
            <foreach collection="dataPermission.deptIds" item="deptId" open="(" close=")" separator=",">
                #{deptId}
            </foreach>
            <if test="dataPermission.viewId == 'ALL'">
                or sou.user_id = #{userId}
            </if>
            )
        </if>

        where o.organization_id = #{orgId}

        <if test="dataPermission != null and dataPermission.self">
            and o.owner = #{userId}
        </if>

        <if test="request.keyword != null and request.keyword != ''">
            and (o.internal_project_no like concat('%', #{request.keyword},'%')
            or o.outsourcing_vendor like concat('%', #{request.keyword},'%')
            or o.experiment_type like concat('%', #{request.keyword},'%'))
        </if>

        <include refid="filters">
            <property name="filters" value="request.filters"/>
        </include>

        <include refid="combine">
            <property name="combineSearch" value="request.combineSearch"/>
        </include>
        <include refid="viewCombine">
            <property name="combineSearch" value="request.viewCombineSearch"/>
        </include>

        <include refid="sort">
            <property name="sort" value="request.sort"/>
        </include>
    </select>

    <select id="getListByIds" resultType="cn.cordys.crm.outsourcing.dto.response.OutsourcingListResponse">
        select o.id, o.internal_project_no, o.project_source, o.experiment_content,
        o.experiment_type, o.outsourcing_vendor, o.deal_price, o.outsourcing_amount,
        o.outsourcing_time, o.result_return_time, o.settlement_time, o.settlement_amount,
        o.follow_up_process, o.owner, o.create_time, o.update_time, o.create_user, o.update_user
        from outsourcing o

        <if test="dataPermission != null and dataPermission.deptIds.size() > 0">
            join sys_organization_user sou on o.owner = sou.user_id
            and (
            sou.department_id in
            <foreach collection="dataPermission.deptIds" item="deptId" open="(" close=")" separator=",">
                #{deptId}
            </foreach>
            )
        </if>

        where o.organization_id = #{orgId}
        and o.id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>

        <if test="dataPermission != null and dataPermission.self">
            and o.owner = #{userId}
        </if>
    </select>

    <sql id="sort">
        <choose>
            <when test="${sort} != null and ${sort}.valid()">
                <bind name="sortType" value="${sort}.type"/>
                <bind name="sortName" value="${sort}.name"/>
                order by
                <choose>
                    <when test="${sort}.name in
                        {'internal_project_no', 'project_source', 'experiment_type', 'outsourcing_vendor',
                         'deal_price', 'outsourcing_amount', 'outsourcing_time', 'result_return_time',
                         'settlement_time', 'settlement_amount', 'follow_up_process', 'owner',
                         'create_time', 'update_time', 'create_user', 'update_user'}">
                        o.${sortName} ${sortType}, o.id ASC
                    </when>
                    <when test="${sort}.name == 'department_id'">
                        sou_sort.${sortName} ${sortType}, o.id ASC
                    </when>
                    <otherwise>
                        CASE
                        WHEN module_table.type = 'INPUT_NUMBER'
                        THEN sort_table.field_value + 0
                        END ${sortType},
                        sort_table.field_value ${sortType},
                        o.id ${sortType}
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                order by o.create_time desc, o.id asc
            </otherwise>
        </choose>
    </sql>

    <sql id="fieldConditionJoin">
        <if test="${conditions} != null and ${conditions}.size() > 0">
            <foreach collection="${conditions}" item="condition" index="i">
                <choose>
                    <when test="condition.refFiled">
                       <include refid="cn.cordys.common.mapper.CommonMapper.refFieldConditionJoin">
                           <property name="condition" value="condition"/>
                           <property name="mainTable" value="outsourcing"/>
                           <property name="mainTableAlias" value="o"/>
                           <property name="fieldTable" value="${tablePrefix}_${i}"/>
                       </include>
                    </when>
                    <otherwise>
                        <if test="condition.customField">
                            <if test="condition.blob">
                                left join outsourcing_field_blob ${tablePrefix}_${i}
                                on o.id = ${tablePrefix}_${i}.resource_id and ${tablePrefix}_${i}.field_id = #{condition.name}
                            </if>
                            <if test="!condition.blob">
                                left join outsourcing_field ${tablePrefix}_${i}
                                on o.id = ${tablePrefix}_${i}.resource_id and ${tablePrefix}_${i}.field_id = #{condition.name}
                            </if>
                        </if>
                        <if test="condition.name == 'departmentId'">
                            left join sys_organization_user ${tablePrefix}_${i}
                            on ${tablePrefix}_${i}.user_id = o.owner
                        </if>
                    </otherwise>
                </choose>
            </foreach>
        </if>
    </sql>

    <sql id="fieldSortJoin">
        <if test="${sort} != null and ${sort}.valid()">
            <bind name="sortName" value="${sort}.name"/>
            <choose>
                <when test="${sort}.name == 'department_id'">
                    left join sys_organization_user sou_sort on o.owner = sou_sort.user_id
                </when>
                <when test="${sort}.name not in
                        {'internal_project_no', 'project_source', 'experiment_type', 'outsourcing_vendor',
                         'deal_price', 'outsourcing_amount', 'outsourcing_time', 'result_return_time',
                         'settlement_time', 'settlement_amount', 'follow_up_process', 'owner',
                         'create_time', 'update_time', 'create_user', 'update_user'}">
                    left join outsourcing_field sort_table
                    on o.id = sort_table.resource_id and sort_table.field_id = #{sortName}
                    left join sys_module_field module_table on sort_table.field_id = module_table.id
                </when>
            </choose>
        </if>
    </sql>

    <sql id="filters">
        <if test="${filters} != null and ${filters}.size() > 0">
            <trim prefix="and">
                <foreach collection="${filters}" item="condition" open="(" close=")" index="i" separator="and">
                    <include refid="condition">
                        <property name="condition" value="condition"/>
                        <property name="fieldTable" value="filter_${i}.field_value"/>
                        <property name="table" value="filter_${i}"/>
                    </include>
                </foreach>
            </trim>
        </if>
    </sql>

    <sql id="combine">
        <if test="${combineSearch} != null and ${combineSearch}.conditions != null and ${combineSearch}.conditions.size() > 0">
            <trim prefix="and">
                <trim prefix="(" suffix=")" suffixOverrides="and|or">
                    <foreach collection="${combineSearch}.conditions" index="i" item="condition">
                        <include refid="condition">
                            <property name="condition" value="condition"/>
                            <property name="fieldTable" value="combine_${i}.field_value"/>
                            <property name="table" value="combine_${i}"/>
                        </include>
                        <include refid="cn.cordys.common.mapper.CommonMapper.searchMode">
                            <property name="searchMode" value="${combineSearch}.searchMode"/>
                        </include>
                    </foreach>
                </trim>
            </trim>
        </if>
    </sql>

    <sql id="viewCombine">
        <if test="${combineSearch} != null and ${combineSearch}.conditions != null and ${combineSearch}.conditions.size() > 0">
            <trim prefix="and">
                <trim prefix="(" suffix=")" suffixOverrides="and|or">
                    <foreach collection="${combineSearch}.conditions" index="i" item="condition">
                        <include refid="condition">
                            <property name="condition" value="condition"/>
                            <property name="fieldTable" value="viewCombine_${i}.field_value"/>
                            <property name="table" value="viewCombine_${i}"/>
                        </include>
                        <include refid="cn.cordys.common.mapper.CommonMapper.searchMode">
                            <property name="searchMode" value="${combineSearch}.searchMode"/>
                        </include>
                    </foreach>
                </trim>
            </trim>
        </if>
    </sql>

    <sql id="condition">
        <choose>
            <when test="condition.refFiled">
                <choose>
                    <when test="!condition.customField">
                        <include refid="cn.cordys.common.mapper.CommonMapper.moduleFieldCondition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="${table}.${condition.name}"/>
                        </include>
                    </when>
                    <otherwise>
                        <include refid="cn.cordys.common.mapper.CommonMapper.moduleFieldCondition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="${fieldTable}"/>
                        </include>
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                <choose>
                    <when test="condition.name == 'internalProjectNo'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.internal_project_no"/>
                        </include>
                    </when>
                    <when test="condition.name == 'projectSource'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.project_source"/>
                        </include>
                    </when>
                    <when test="condition.name == 'experimentType'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.experiment_type"/>
                        </include>
                    </when>
                    <when test="condition.name == 'outsourcingVendor'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.outsourcing_vendor"/>
                        </include>
                    </when>
                    <when test="condition.name == 'dealPrice'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.deal_price"/>
                        </include>
                    </when>
                    <when test="condition.name == 'outsourcingAmount'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.outsourcing_amount"/>
                        </include>
                    </when>
                    <when test="condition.name == 'outsourcingTime'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.outsourcing_time"/>
                        </include>
                    </when>
                    <when test="condition.name == 'resultReturnTime'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.result_return_time"/>
                        </include>
                    </when>
                    <when test="condition.name == 'settlementTime'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.settlement_time"/>
                        </include>
                    </when>
                    <when test="condition.name == 'settlementAmount'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.settlement_amount"/>
                        </include>
                    </when>
                    <when test="condition.name == 'followUpProcess'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.follow_up_process"/>
                        </include>
                    </when>
                    <when test="condition.name == 'owner'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.owner"/>
                        </include>
                    </when>
                    <when test="condition.name == 'createTime'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.create_time"/>
                        </include>
                    </when>
                    <when test="condition.name == 'updateTime'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.update_time"/>
                        </include>
                    </when>
                    <when test="condition.name == 'createUser'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.create_user"/>
                        </include>
                    </when>
                    <when test="condition.name == 'updateUser'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="o.update_user"/>
                        </include>
                    </when>
                    <when test="condition.name == 'departmentId'">
                        <include refid="cn.cordys.common.mapper.CommonMapper.condition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="${table}.department_id"/>
                        </include>
                    </when>
                    <otherwise>
                        <include refid="cn.cordys.common.mapper.CommonMapper.moduleFieldCondition">
                            <property name="condition" value="condition"/>
                            <property name="column" value="${fieldTable}"/>
                        </include>
                    </otherwise>
                </choose>
            </otherwise>
        </choose>
    </sql>
</mapper>
